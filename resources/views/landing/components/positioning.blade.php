<section id="positioning" class="py-24 bg-white px-6">
    <div class="max-w-5xl mx-auto text-center">

        <span class="text-sm font-medium tracking-widest text-amber-400 uppercase">
            {{ __('landing.positioning_badge') }}
        </span>

        <h2 class="mt-4 text-4xl font-bold text-gray-900">
            {{ __('landing.positioning_title') }}
        </h2>

        <p class="mt-6 text-lg text-gray-500 max-w-3xl mx-auto">
            {{ __('landing.positioning_description') }}
        </p>

        <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-10">

            <div class="p-8 border border-gray-100 rounded-2xl shadow-sm">
                <div class="text-4xl mb-4">🏙️</div>
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.positioning_feat1_title') }}</h3>
                <p class="mt-2 text-gray-500 text-sm">{{ __('landing.positioning_feat1_desc') }}</p>
            </div>

            <div class="p-8 border border-gray-100 rounded-2xl shadow-sm">
                <div class="text-4xl mb-4">📈</div>
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.positioning_feat2_title') }}</h3>
                <p class="mt-2 text-gray-500 text-sm">{{ __('landing.positioning_feat2_desc') }}</p>
            </div>

            <div class="p-8 border border-gray-100 rounded-2xl shadow-sm">
                <div class="text-4xl mb-4">🔐</div>
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.positioning_feat3_title') }}</h3>
                <p class="mt-2 text-gray-500 text-sm">{{ __('landing.positioning_feat3_desc') }}</p>
            </div>

        </div>
    </div>
</section>