<section id="approach" class="min-h-screen flex items-center bg-white px-6 py-24">
    <div class="max-w-5xl mx-auto">

        <div class="text-center">
            <span class="text-sm font-medium tracking-widest text-amber-400 uppercase">
                {{ __('landing.approach_badge') }}
            </span>
            <h2 class="mt-4 text-4xl font-bold text-gray-900">
                {{ __('landing.approach_title') }}
            </h2>
            <p class="mt-6 text-lg text-gray-600 max-w-2xl mx-auto">
                {{ __('landing.approach_description') }}
            </p>
        </div>

        <div class="mt-16 grid grid-cols-1 md:grid-cols-2 gap-8">

            <div class="p-8 bg-gray-300 rounded-2xl">
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.approach_item1_title') }}</h3>
                <p class="mt-3 text-gray-500 text-sm">{{ __('landing.approach_item1_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-300 rounded-2xl">
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.approach_item2_title') }}</h3>
                <p class="mt-3 text-gray-500 text-sm">{{ __('landing.approach_item2_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-300 rounded-2xl">
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.approach_item3_title') }}</h3>
                <p class="mt-3 text-gray-500 text-sm">{{ __('landing.approach_item3_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-300 rounded-2xl">
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.approach_item4_title') }}</h3>
                <p class="mt-3 text-gray-500 text-sm">{{ __('landing.approach_item4_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-300 rounded-2xl">
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.positioning_feat2_title') }}</h3>
                <p class="mt-3 text-gray-500 text-sm">{{ __('landing.positioning_feat2_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-300 rounded-2xl">
                <h3 class="text-lg font-semibold text-gray-900">{{ __('landing.positioning_feat3_title') }}</h3>
                <p class="mt-3 text-gray-500 text-sm">{{ __('landing.positioning_feat3_desc') }}</p>
            </div>

        </div>
    </div>
</section>