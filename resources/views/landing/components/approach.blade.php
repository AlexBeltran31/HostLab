<section class="py-24 bg-gray-950 text-white px-6">
    <div class="max-w-5xl mx-auto">

        <div class="text-center">
            <span class="text-sm font-medium tracking-widest text-amber-400 uppercase">
                {{ __('landing.approach_badge') }}
            </span>
            <h2 class="mt-4 text-4xl font-bold">
                {{ __('landing.approach_title') }}
            </h2>
            <p class="mt-6 text-lg text-gray-400 max-w-2xl mx-auto">
                {{ __('landing.approach_description') }}
            </p>
        </div>

        <div class="mt-16 grid grid-cols-1 md:grid-cols-2 gap-8">

            <div class="p-8 bg-gray-900 rounded-2xl">
                <h3 class="text-lg font-semibold text-amber-400">{{ __('landing.approach_item1_title') }}</h3>
                <p class="mt-3 text-gray-400 text-sm">{{ __('landing.approach_item1_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-900 rounded-2xl">
                <h3 class="text-lg font-semibold text-amber-400">{{ __('landing.approach_item2_title') }}</h3>
                <p class="mt-3 text-gray-400 text-sm">{{ __('landing.approach_item2_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-900 rounded-2xl">
                <h3 class="text-lg font-semibold text-amber-400">{{ __('landing.approach_item3_title') }}</h3>
                <p class="mt-3 text-gray-400 text-sm">{{ __('landing.approach_item3_desc') }}</p>
            </div>

            <div class="p-8 bg-gray-900 rounded-2xl">
                <h3 class="text-lg font-semibold text-amber-400">{{ __('landing.approach_item4_title') }}</h3>
                <p class="mt-3 text-gray-400 text-sm">{{ __('landing.approach_item4_desc') }}</p>
            </div>

        </div>
    </div>
</section>